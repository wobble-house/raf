A headless CMS built for A2IM to supply media content to all web properties.  This server is built with AWS services such as EC2, DynamoDB, & S3.  The underlying framework used to build the server is Strapi.  Strapi is capable of  utilizing both RESTful & GraphQL API simultaneously.